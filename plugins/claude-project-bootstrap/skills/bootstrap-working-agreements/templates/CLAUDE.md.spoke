# `<subdir-name>` — Claude guidance

**If also touching `<other-subdir>` code, refer to:** `<other-subdir>/CLAUDE.md` for `<what>` (not auto-loaded in this subtree's sessions) · `.claude/rules/<topic>.md` for `<topic>` · [`<project>` GitHub Project](<project-url>) (filter `Area: <this-area>`) for pending work in this subtree

`<one paragraph: framework, dev server URL, how this subtree fits into the whole. Example (pick a flavor close to your stack): "Express + Prisma + Postgres. API on http://localhost:3001. Verify via curl /api/health and the vitest suite under server/__tests__/.">`

`<optional: pending-work callout for foundational things missing that future-Claude must not assume exist. Example: "Pending: server/jobs/scheduler.ts is a stub — exports the type but throws on call. Do not wire callers to it yet.">`

## `<Topic 1: e.g. data flow / component status / module status>`

`<concrete table or fenced diagram. Pick the format that fits this subtree: status table for components, ASCII data-flow for pipelines, API surface map for libraries.>`

| `<Component / Module>` | Status | Notes |
|---|---|---|
| `<name>` | Done | `<one-line: what it does>` |
| `<name>` | Partial | `<what's done, what's missing>` |
| `<name>` | Mock | `<note: uses static data; wiring pending>` |

## `<Topic 2: a specific gotcha or convention only relevant in this subtree>`

`<concrete rule. Example: "All user-facing IDs in this package MUST be ULID, not UUIDv4 — the database has a unique index on the ULID timestamp prefix that breaks under v4. A migration in 2024 silently dropped 12 rows when this was violated.">`

## `<Topic 3: a non-obvious constraint or decision>`

`<concrete explanation with the why. Example: "Background-job poll interval is 750ms, not 1s — the upstream rate limiter resets on the second boundary, and aligning with it produced bursty 429s. The 250ms offset spreads load.">`

## Landmines

`<bullet list. Each entry: file path · symptom · fix. Example: "src/components/Chart.tsx: imports a chart library icon that doesn't exist in v0.x — silent white screen, no console error. Pin to v1.2 and use the named export. Verify with: node -e \"require('chart-lib').iconExists('foo')\".">`

## Deferred features

See the [`<project>` GitHub Project](<project-url>) (filter `Area: <this-area>`) for all pending work in this subtree.
